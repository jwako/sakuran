<div class="row">
<%= paginate @tweets %>
</div>

<div class="row">
<% @tweets.each do |tweet| %>
<div class="col-xs-12 col-sm-6 col-md-2">
	<%= link_to image_tag(tweet.url, class: "img-thumbnail img-responsive"), admin_path(tweet) %>
</div>
<% end %>
</div>

<div class="row">
<%= paginate @tweets %>
</div>